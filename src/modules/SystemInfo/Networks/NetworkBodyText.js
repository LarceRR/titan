export default function NetworkBodyText(props) {
    return (
        <div className="NetworkBodyText">
            <span>{props.title}</span>
            <span>{props.info}</span>
        </div>
    )
}