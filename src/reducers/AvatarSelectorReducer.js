// import { SET_AVATAR_SELECTOR } from "../actions/actions";
// import { createAction } from '@reduxjs/toolkit';

// export const setAvatarSelector = createAction(SET_AVATAR_SELECTOR);

// export default function AvatarSelectorReducer(state = {value:''}, action) {
//   switch (action.type) {
//     case MODAL_OPEN_STATE:
//       return {value: !state.value}  
//     default:
//       return state
//   }
// }